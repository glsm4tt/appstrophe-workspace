import * as fromAuth from './reducers';

describe('Auth Selectors', () => {
  it('should select the feature state', () => {
 /*   const result = selectAuth({
      [fromAuth.AUTH_FEATURE_KEY]: {}
    });
    expect(result).toEqual(undefined);*/
    expect(true).toBe(true);
  });
});
