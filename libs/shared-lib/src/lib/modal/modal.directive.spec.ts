import { PopoverDirective } from './modal.directive';

describe('PopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverDirective();
    expect(directive).toBeTruthy();
  });
});
